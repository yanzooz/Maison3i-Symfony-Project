<footer class="bg-white border-t border-gray-200 text-gray-800 mt-20">
  <div class="max-w-7xl mx-auto px-6 py-16 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-10 text-sm">

    <!-- Bloc logo + description -->
    <div>
      <img src="{{ asset('image/assets/logoMaison3iSite.png') }}" alt="Logo MAISON3I" class="h-12 w-auto mb-4">
      <p class="text-gray-600 leading-relaxed text-sm">
        Marque indépendante fondée à Lyon.<br>
        Édition limitée. Style affirmé.
      </p>
    </div>

    <!-- Bloc navigation -->
    <div>
      <h2 class="text-base font-semibold uppercase tracking-wide mb-4">Navigation</h2>
      <ul class="space-y-2">
        <li><a href="{{ path('app_home') }}" class="hover:text-indigo-600 transition">Accueil</a></li>
        <li><a href="{{ path('app_products') }}" class="hover:text-indigo-600 transition">Produits</a></li>
        {% if not app.user %}
          <li><a href="{{ path('app_register') }}" class="hover:text-indigo-600 transition">Inscription</a></li>
          <li><a href="{{ path('app_login') }}" class="hover:text-indigo-600 transition">Connexion</a></li>
        {% else %}
          <li><a href="{{ path('app_logout') }}" class="hover:text-red-600 transition">Déconnexion</a></li>
        {% endif %}
      </ul>
    </div>

    <!-- Bloc réseaux sociaux -->
    <div>
      <h2 class="text-base font-semibold uppercase tracking-wide mb-4">Suivez-nous</h2>
      <ul class="flex flex-col gap-2 text-gray-500">
        <li>
          <a href="#" class="hover:text-indigo-600 transition flex items-center gap-2">
            <i class="fab fa-instagram"></i> Instagram
          </a>
        </li>
        <li>
          <a href="#" class="hover:text-indigo-600 transition flex items-center gap-2">
            <i class="fab fa-tiktok"></i> TikTok
          </a>
        </li>
        <li>
          <a href="#" class="hover:text-indigo-600 transition flex items-center gap-2">
            <i class="fab fa-x-twitter"></i> Twitter
          </a>
        </li>
      </ul>
    </div>

    <!-- Bloc newsletter -->
    <div>
      <h2 class="text-base font-semibold uppercase tracking-wide mb-4">Newsletter</h2>
      <form method="post" class="space-y-3">
        <input type="email" placeholder="Votre email"
               class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:border-indigo-500">
        <button type="submit"
                class="w-full bg-gray-900 text-white py-2 rounded-md hover:bg-gray-700 transition">
          S’inscrire
        </button>
      </form>
    </div>
  </div>

  <!-- Copyright -->
  <div class="text-center text-xs text-gray-500 border-t py-4">
    &copy; {{ "now"|date("Y") }} Maison3i — Tous droits réservés.
  </div>
</footer>
